/**
 * STUB / clase base.
 *
 * Luego puede reemplazarse por storage real o local.
 */
class ScreenshotStorage {
  async save(imageBuffer, filename) {
    return "STUB_SCREENSHOT_URL";
  }
}

module.exports = ScreenshotStorage;
